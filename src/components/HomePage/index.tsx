import { HomePageWrapper, Title } from './components';

const HomePage: React.FC = () => (
  <HomePageWrapper>
    <Title>All your expenses, in one <span>app</span></Title>
  </HomePageWrapper>
);

export default HomePage;
